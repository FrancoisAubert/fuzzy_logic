#pragma once
#include <>
#define _CRT_SECURE_NO_WARNINGS
#include <iostream>
#include <fstream>
#include <cstring>
#define MAX_CHARS_PER_LINE = 512;
#define MAX_TOKENS_PER_LINE = 20;
#define  DELIMITER = " ";



class fait{

std::string nom;
bool flou;
int prem [];
int concl [];
int metaprem [];
bool eval;
std::string val;
double coeff;


Fait(std::string anom, string aval, double acoeff){
nom = anom;
val = aval;
coeff = acoeff;
prem = int[];
concl = int[];
metaprem = int[];
flou = false;
eval = false;
}

Fait(std::string nom, bool flou, int prem[], int concl[], int metaprem[], bool eval, double, val, double coeff){};


std::pair<std::string val, double coeff> lire(std::string, xml ){

	

}


}